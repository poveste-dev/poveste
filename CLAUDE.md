# Poveste — Claude Code

The full AI contributor guide lives in `ai/AGENTS.md` and is shared by Claude, Codex and Cursor. It is imported below; the skills in `ai/skills/` are available as `/add-conformance-story`, `/open-pr`, `/file-an-issue` and `/cut-a-release`.

@ai/AGENTS.md
